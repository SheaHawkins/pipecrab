[package]
name = "pipecrab-core"
description = "Sans-IO core for pipecrab: frames, processors. No async, no I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# Architectural layer for the `layering` gate (crates/support/arch). A package
# may depend only on strictly lower layers: core < runtime < {trait, facade}.
[package.metadata.pipecrab]
layer = "core"

[dependencies]

[dev-dependencies]
pipecrab-test-util = { workspace = true }
