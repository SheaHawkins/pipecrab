[package]
name = "pipecrab-turn"
description = "Turn policy for pipecrab: the BargeInStage that turns user speech onset into a downstream Interrupt."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[package.metadata.pipecrab]
layer = "trait"

[dependencies]
pipecrab-core    = { workspace = true }
pipecrab-runtime = { workspace = true }
async-trait      = "0.1"

[dev-dependencies]
futures            = "0.3"
pipecrab-test-util = { workspace = true }
